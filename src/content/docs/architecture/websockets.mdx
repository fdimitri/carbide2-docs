---
title: WebSocket Protocol
description: EventMachine worker commandSet architecture and message format.
sidebar:
  order: 3
---

## Wire format

All WebSocket messages are JSON objects with three top-level fields:

```json
{
  "cs":      "fs",
  "cmd":     "write",
  "payload": { "path": "/src/app.js", "delta": "..." }
}
```

The current worker does not implement a top-level request id or `ref` field.

| Field | Type | Description |
|-------|------|-------------|
| `cs` | string | CommandSet — routes to the correct handler module |
| `cmd` | string | Command within that commandSet |
| `payload` | object | Command-specific data |

## Server → client messages

Responses and broadcasts use the same format:

```json
{
  "cs":      "fs",
  "cmd":     "tree",
  "payload": { "tree": { "path": "/", "children": [] } }
}
```

## CommandSets

### `system`

Connection lifecycle and metadata.

| Command (server → client) | Description |
|--------------------------|-------------|
| `connected` | Initial handshake — carries `protocol`, `min_client`, `project_id` |
| `error` | Unrecoverable error |

### `fs`

DBFS operations. See [Filesystem (DBFS)](/architecture/filesystem/).

| Command | Direction | Description |
|---------|-----------|-------------|
| `tree` | C→S / S→C | List directory entries |
| `read` | C→S / S→C | Read file content |
| `open` | C→S | Register as viewing a file |
| `cursor` | C→S / S→C | Update and broadcast editor cursor position |
| `write` | C→S | Append one or more DBFS change records |
| `set_contents` | C→S | Replace full content |
| `create_file` | C→S | Create a new file |
| `create_dir` | C→S | Create a directory |
| `rename` | C→S | Rename a path |
| `delete` | C→S | Delete an entry |
| `change` | S→C | Broadcast a file change to other viewers |

### `terminal`

PTY terminal multiplexing.

In Kubernetes mode, terminal PTYs are executed via a per-project shell sidecar
backend. This keeps terminal toolsets isolated from the server/worker runtime
and constrains filesystem access to the mounted project root.

| Command | Direction | Description |
|---------|-----------|-------------|
| `create` | C→S | Create a new PTY terminal |
| `join` | C→S | Subscribe to an existing terminal |
| `input` | C→S | Send keystrokes to PTY |
| `output` | S→C | Broadcast PTY output to subscribers |
| `resize` | C→S | Resize PTY (cols × rows) |
| `destroy` | C→S | Close a terminal session |

#### Terminal backend modes

| Backend | Execution model | Isolation level |
|---------|------------------|-----------------|
| `local` | `PTY.spawn` directly in workspace pod process space | Lowest (shared runtime namespace) |
| `docker` | `docker exec` into project container | Strong (container boundary) |
| `kube` | `kubectl exec` into project shell sidecar/pod | Strong (pod/container boundary + project-root mount) |

Default production posture is sidecar-backed terminals so tool execution stays
out of the server/worker process namespaces.

### `chat`

IRC-style project chat.

| Command | Direction | Description |
|---------|-----------|-------------|
| `join` | C→S | Join a channel room |
| `message` | C→S / S→C | Post / receive a message |
| `typing` | C→S / S→C | Typing indicator |
| `leave` | C→S / S→C | Leave a channel room |

### `agent`

AI agent interaction.

| Command | Direction | Description |
|---------|-----------|-------------|
| `list` / `recent` / `load` | C→S | List agents or load conversation state |
| `ask` | C→S | Send a prompt to an agent |
| `tool_call` / `tool_result` | S→C | Surface tool activity for a turn |
| `done` | S→C | Final assistant response for the turn |
| `error` | S→C | Agent-turn failure |

## Protocol versioning

The worker advertises two constants on the `system/connected` message:

| Field | Description |
|-------|-------------|
| `protocol` | Current server protocol version (integer) |
| `min_client` | Minimum client version the server accepts |

The client sends `&proto=N&min_server=M` on the WebSocket URL. A mismatch
currently logs a warning (non-blocking); hard-fail will be added in a future release.

## Source files

| File | Role |
|------|------|
| `worker/worker.rb` | Main EM server; routes `cs` to handlers; protocol constants |
| `worker/fs_store.rb` | `fs` commandSet handler |
| `worker/handlers/chat_handlers.rb` | `chat` commandSet handler |
| `worker/handlers/term_handlers.rb` | `term` commandSet + PTY |
| `worker/handlers/agent_handlers.rb` | `agent` commandSet handler |
